@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">{{ __('Dashboard') }}
                    <p class="text-right"><span><a href="{{url('/')}}"  class=" btn btn-outline-info">Home</a></span> 
                        <span><a href="{{url('/unknown')}}" class=" btn btn-outline-info">Check Messages</a></span> </p>
                </div>

                <div class="card-body">
                    @if (session('status'))
                        <div class="alert alert-success" role="alert">
                            {{ session('status') }}
                        </div>
                    @endif

                    {{ __('You are logged in!') }}
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
